spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookstore_dev?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root  # Your MySQL username
    password: VRvijay@13 # Your MySQL password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # Update schema as entities change
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  data:
    redis:
      host: localhost      # Redis server address
      port: 6379          # Default Redis port
      timeout: 2000ms
